It’s a lock that separates read access and write access so that:

Multiple readers can access data at the same time

Only one writer can modify data

Writers get exclusive access

This improves performance in read-heavy systems.